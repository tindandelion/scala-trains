import org.scalatest.FunSpec

class TrainsAcceptanceSpec extends FunSpec {
  describe("initial configurations") {
    it("reports crash when 2 trains start from the same station")(pending)
    it("reports success when 2 trains start from different stations")(pending)
  }
}
